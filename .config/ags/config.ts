const config = {
  devMode: true,
  notifications: {
    enabled: true,
    dev: false,
    text: {
      maxSummaryLength: 30,
      maxBodyLength: 40,
    },
    defaultTimeoutMs: 3000,
  },
  bar: {
    enabled: true,
  },
  player: {
    enabled: false,
  }
};

export default config;
